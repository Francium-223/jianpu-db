# dup-identical —— 同源、整段 score 逐字相同的重复拷贝(只移不删)
保留规则: 名字更像曲名(CJK 优先, 无 `_2`/`&nbsp;` 痕迹) > 标签多 > 文件名短。
由 `jianpu2/tools/audit_corpus_quality.py --apply-dup` 移入, 时间 2026-09-24 02:11。

- 去掉 `1=F4_4_深.txt`(1=F4_4_深) —— 与 `牧羊姑娘1=F4_4_深情悠扬地.txt` 逐字相同, 同 source jianpujia-315435
- 去掉 `上海人&nbsp;&nbsp;.txt`(上海人) —— 与 `上海人.txt` 逐字相同, 同 source jianpucn-475629
- 去掉 `中__年.txt`(中__年) —— 与 `中国少年先锋队队歌.txt` 逐字相同, 同 source jianpujia-26442
- 去掉 `中秋情.txt`(中秋情) —— 与 `中秋情_&nbsp;&nbsp;.txt` 逐字相同, 同 source jianpucn-475610
- 去掉 `丽_2.txt`(丽) —— 与 `美丽的草原.txt` 逐字相同, 同 source jianpujia-313802
- 去掉 `亲_2.txt`(亲) —— 与 `母亲草原刘志毅词张瑞孔徐书民曲.txt` 逐字相同, 同 source jianpujia-358388
- 去掉 `以.txt`(以) —— 与 `难以把你放下草原丁喜萨斯.txt` 逐字相同, 同 source jianpujia-238455
- 去掉 `八角树&nbsp;&nbsp;.txt`(八角树) —— 与 `八角树.txt` 逐字相同, 同 source jianpucn-475601
- 去掉 `公鸡和母鸡钢琴.txt`(公鸡和母鸡钢琴) —— 与 `鸡.txt` 逐字相同, 同 source jianpujia-246118
- 去掉 `太湖水&nbsp;&nbsp;.txt`(太湖水) —— 与 `太湖水.txt` 逐字相同, 同 source jianpucn-475893
- 去掉 `学.txt`(学) —— 与 `学习雷锋好榜样.txt` 逐字相同, 同 source jianpujia-26520
- 去掉 `学厨艺&nbsp;&nbsp;.txt`(学厨艺) —— 与 `学厨艺.txt` 逐字相同, 同 source jianpucn-475660
- 去掉 `学工歌&nbsp;&nbsp;.txt`(学工歌) —— 与 `学工歌.txt` 逐字相同, 同 source jianpucn-475663
- 去掉 `学电脑&nbsp;&nbsp;.txt`(学电脑) —— 与 `学电脑.txt` 逐字相同, 同 source jianpucn-475615
- 去掉 `宜昌队&nbsp;&nbsp;.txt`(宜昌队) —— 与 `宜昌队.txt` 逐字相同, 同 source jianpucn-475898
- 去掉 `寄明月&nbsp;&nbsp;.txt`(寄明月) —— 与 `寄明月.txt` 逐字相同, 同 source jianpucn-475792
- 去掉 `小上海&nbsp;&nbsp;.txt`(小上海) —— 与 `小上海.txt` 逐字相同, 同 source jianpucn-475628
- 去掉 `小松树.txt`(小松树) —— 与 `未命名-jianpujia-74558.txt` 逐字相同, 同 source jianpujia-74558
- 去掉 `小重山&nbsp;&nbsp;.txt`(小重山) —— 与 `小重山.txt` 逐字相同, 同 source jianpucn-475594
- 去掉 `少年游.txt`(少年游) —— 与 `少年游_&nbsp;&nbsp;.txt` 逐字相同, 同 source jianpucn-475749
- 去掉 `山.txt`(山) —— 与 `高高山上四川民歌、周亚平填词.txt` 逐字相同, 同 source jianpujia-420215
- 去掉 `康.txt`(康) —— 与 `康定情歌.txt` 逐字相同, 同 source jianpujia-299366
- 去掉 `当兵乐&nbsp;&nbsp;.txt`(当兵乐) —— 与 `当兵乐.txt` 逐字相同, 同 source jianpucn-475802
- 去掉 `当我们同在一起.txt`(当我们同在一起) —— 与 `当我们同在一起_2.txt` 逐字相同, 同 source jianpujia-74561
- 去掉 `忘情酒&nbsp;&nbsp;.txt`(忘情酒) —— 与 `忘情酒.txt` 逐字相同, 同 source jianpucn-475596
- 去掉 `意难平&nbsp;&nbsp;.txt`(意难平) —— 与 `意难平.txt` 逐字相同, 同 source jianpucn-475825
- 去掉 `我的梦&nbsp;&nbsp;.txt`(我的梦) —— 与 `我的梦.txt` 逐字相同, 同 source jianpucn-475668
- 去掉 `打电话.txt`(打电话) —— 与 `打电话_2.txt` 逐字相同, 同 source jianpujia-42563
- 去掉 `把爱留在草原高佳词车若娟曲.txt`(把爱留在草原高佳词车若娟曲) —— 与 `把爱留在草原高佳词车若娟曲_2.txt` 逐字相同, 同 source jianpujia-217566
- 去掉 `推车歌&nbsp;&nbsp;.txt`(推车歌) —— 与 `推车歌.txt` 逐字相同, 同 source jianpucn-475917
- 去掉 `木叶情&nbsp;&nbsp;.txt`(木叶情) —— 与 `木叶情.txt` 逐字相同, 同 source jianpucn-475637
- 去掉 `歌友情&nbsp;&nbsp;.txt`(歌友情) —— 与 `歌友情.txt` 逐字相同, 同 source jianpucn-475556
- 去掉 `歌唱二小放牛郎.txt`(歌唱二小放牛郎) —— 与 `歌唱二小放牛郎_2.txt` 逐字相同, 同 source jianpujia-74240
- 去掉 `泼水歌.txt`(泼水歌) —— 与 `泼水歌_2.txt` 逐字相同, 同 source jianpujia-74234
- 去掉 `爱是一首歌.txt`(爱是一首歌) —— 与 `爱是一首歌_2.txt` 逐字相同, 同 source jianpujia-26490
- 去掉 `男儿泉&nbsp;&nbsp;.txt`(男儿泉) —— 与 `男儿泉.txt` 逐字相同, 同 source jianpucn-475607
- 去掉 `绿.txt`(绿) —— 与 `绿豆青河源民歌老隆民歌.txt` 逐字相同, 同 source jianpujia-240789
- 去掉 `胭脂妆&nbsp;&nbsp;.txt`(胭脂妆) —— 与 `胭脂妆.txt` 逐字相同, 同 source jianpucn-475872
- 去掉 `舀河浜&nbsp;&nbsp;.txt`(舀河浜) —— 与 `舀河浜.txt` 逐字相同, 同 source jianpucn-475614
- 去掉 `苹果香&nbsp;&nbsp;.txt`(苹果香) —— 与 `苹果香.txt` 逐字相同, 同 source jianpucn-475453
- 去掉 `菜园子&nbsp;&nbsp;.txt`(菜园子) —— 与 `菜园子.txt` 逐字相同, 同 source jianpucn-475520
- 去掉 `达.txt`(达) —— 与 `邦达草原知音草原风情系列之四.txt` 逐字相同, 同 source jianpujia-236319
- 去掉 `陆北大&nbsp;&nbsp;.txt`(陆北大) —— 与 `陆北大.txt` 逐字相同, 同 source jianpucn-475623
- 去掉 `青原赋.txt`(青原赋) —— 与 `青原赋_&nbsp;&nbsp;.txt` 逐字相同, 同 source jianpucn-475606
- 去掉 `马头.txt`(马头) —— 与 `马头琴草原风.txt` 逐字相同, 同 source jianpujia-325095
