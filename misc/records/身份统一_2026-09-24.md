# 提交身份统一成 Francium-223（2026-09-24 12:50）

## 原来错在哪

我这一夜的提交用了几个**假身份**（为了区分"哪个库的提交"），这是错的做法 —— 它们不是你：

| 库 | 假身份 | 条数 |
|---|---|---|
| jianpu-db | `jianpu-db <db@local>` | 42 |
| jianpu-db | `jianpu2 <jianpu2@local>`（一条漏网的） | 1 |
| jianpu2 | `jianpu2 <jp2@local>` | 23 |
| jianpu-web | `jianpu-web <web@local>` | 5 |

## 怎么改的

1. 每个库先建**本地备份分支** `backup-authorfix-20260924`（指向重写前的 HEAD，**只在本机、不推**）；
2. `git filter-branch --env-filter`：**只**把上面那几个假邮箱的 author/committer 换成
   `Francium-223 <wizardofyendor@outlook.com>`（你自己的提交、以及 `github-actions[bot]` 的 13 条 CI 提交**原样保留**）；
3. 三个库都设了**本地 git 配置**：`user.name=Francium-223` / `user.email=wizardofyendor@outlook.com`
   —— 以后在这个库里提交（包括我干的）都会是你的身份；
4. 强推前先确认远端**没有**我备份之外的提交（0 条）→ `git push --force-with-lease` 安全推送。

## 结果

| 库 | 重写前 | **重写后（远端 = 本地）** | 远端作者 |
|---|---|---|---|
| jianpu-db | `c1fdd152` | **`1799e7d8`** | Francium-223 ✓ + CI bot 13 |
| jianpu2 | `9107750` | **`aed6d1f`** | Francium-223 ✓ |
| jianpu-web | `73aaeab` | **`a87c0fe`** | Francium-223 ✓ |

## 两个要知道的后果

1. **历史哈希全变了**：本记录之前所有文档里写的短哈希（`b55054f0`、`e566917f`、`d46a55aa` …）**都已作废**，
   以本表为准。备份分支还留着重写前的那份历史，随时可比对。
2. `jianpu-web/app/server.py` 里**读者投稿**的运行时提交身份仍是 `reader-submit <submit@local>`
   —— 那是我故意留的"来源标记"（区分"作者自己提交"与"读者从网页投的"），**没有改**。
   你要是不想要这个标记，说一声，我把它也换成你的身份。

*（本记录由 DSH 于 2026-09-24 12:50 生成。）*
