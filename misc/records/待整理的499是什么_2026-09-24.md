# `parse_scores.py` 里那个"待整理已跳过 499"到底是什么（2026-09-24 11:00）

> 每次跑 `parse_scores.py` 都会看到 `data.jsonl 7321 首 … (待整理已跳过 499)`。
> 499 是 **7,820 个曲谱文件 − 7,321 首进数据集**的差；**查清了，没有丢数据**，三类：

| 类别 | 数量 | 是什么 | 有音符吗 | 该怎么办 |
|---|---|---|---|---|
| `status=midi` | **273** | MIDI 硬转过来的东方曲（th02–th12） | 有，但**中位数 11,242 音、最大 175,392 音**，合计 **474 万音 = 现有语料的 3.6 倍** | **必须继续排除**（它们是**多轨转储**，收进来会把 1.3M 音的语料变成 6M 音、并把检索搅烂） |
| 没有 `status` | **225** | 东方曲目的**元数据占位**（th075/th08/th095/th105…：只有 title/tag/usertag/tagroute/MBID/alias） | **0 个音符**（`%---` 之后直接 `%END`） | 保持现状（等有谱了再说） |
| 空 `score` | 1 | 同上性质 | 0 | 保持现状 |

**白名单本身没问题**：`parse_scores.py` 只收 `status ∈ {ok, ocr}`，而且实测**没有任何大小写/空格变体**
（不是 `OCR`/`ok ` 这种被误伤的情况）；`midi` 与"无 status"两类总共 498 + 1 个空谱 = 499 ✓ 完全对上。

## 能顺手做的（可选，收益不大）

`status=midi` 那 273 个里，**25 个 ≤ 200 音**（正常转录量级），但其中有一批是 **1 个音的残渣**
（`th08_02`…`th08_09` 都是 1 音）。也就是说"体积正常、可能是真旋律"的**顶多二三十首**，
而且它们是 MIDI 来源（不是本仓库要的简谱记录口径）。**我的建议：不动。**
真要补东方曲目，走"找简谱扫描件 → 转写"的正路（锦囊 §4），别开 MIDI 这个口子。

## 钉成不变量了

语料自检门加第 **7** 条：**`data.jsonl` 里不许出现 `status=midi` / 无 status / 空谱**
（实测 `midi 0, 无 status 0, 空谱 0` ✓）。以后谁改白名单放它们进来，自检立刻红。

*（本记录由 DSH 于 2026-09-24 11:00 生成。）*
