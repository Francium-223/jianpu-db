# 醒来先看这个（2026-09-24 09:20）

## 0. 三十秒现状

| 项 | 状态 |
|---|---|
| 语料 | **7,321 首 / 1,301,430 音高音 / 336,414 小节**；无标签 **910** 首（10:30 由站内栏目补了 31 首，见下） |
| 服务 | `http://127.0.0.1:8770/` `active`、开机自启 `enabled`、`Linger=yes`（**退出登录也活着**）、`kill -9` 后 **5 秒自愈** |
| 自检 | 语料门 6/6；工具链四层（冒烟／静态 593 文件／三条写盘功能／解析副作用）；前端+接口 6 组；前后端等价 —— **全绿** |
| 仓库 | 三个库工作区全干净；`jianpu-db` 领先 `Francium-223/master` **40** 个提交 |
| 榜单 | 4 清单 Top-1 = 100% / 98.9% / 98.3% / 100%（与昨夜基线**逐项一致**，无回归） |

## 1. push：**三个库全部推送完成** ✓（2026-09-24 12:35）

| 库 | 远端 = 本地 | 状态 |
|---|---|---|
| `jianpu-db` | `2111c3ee` | 领先 0 / 落后 0 ✓（推完等 60 秒，CI 没产生额外提交 → 语料一致） |
| `jianpu2` | `9107750` | 领先 0 / 落后 0 ✓（首次推送，190MB） |
| `jianpu-web` | `73aaeab` | 领先 0 / 落后 0 ✓ |

* 三个库的 remote 都换成 **SSH**（`git@github.com:Francium-223/...`），用的是你新配的 key（实测 `Hi Francium-223!` ✓）
* 途中修了自己一个探测 bug：`git ls-remote --exit-code` 对**空仓库**会返回 2 → 把"刚建好的空仓库"误判成"不存在"（`推送.sh` 已改）
* **只剩 HuggingFace 上传**：把 `hf_xxx` 写进 `~/.config/jianpu/hf_token`（或 `_analysis/.hf_token`）后
  `bash _analysis/上传HF.sh` 即可；另外 `jianpu-db` 里有个 `publish-hf.yaml` 工作流，若在仓库
  Settings → Secrets 里加一个 `HF_TOKEN`，以后 push 就能自动发布。细节：`_analysis/凭据怎么给.md`。

> ⚠️ **12:50 统一了提交身份**：我这一夜的提交原来挂着 `jianpu-db <db@local>` 这类**假身份**，
> 已全部改成 `Francium-223 <wizardofyendor@outlook.com>`（你自己和 CI bot 的提交原样保留）。
> 三个库都设了本地 git 配置，**以后提交都是你的身份**。历史被重写，**哈希全变了**：
> `jianpu-db 1799e7d8`（+ 之后一条 docs）/ `jianpu2 aed6d1f` / `jianpu-web a87c0fe`；
> 本文档此前提到的短哈希（`b55054f0`、`d46a55aa` 等）**作废**。备份分支 `backup-authorfix-20260924` 只在本机。
> 详见 `_analysis/身份统一_2026-09-24.md`。

## 2. 等你拍板的五件事（按"能带来多少"排序）

| # | 事项 | 现在多少 | 证据 / 入口 | 怎么做 | 我的建议 |
|---|---|---|---|---|---|
| 1 | **补标签** | **910 首**一个标签都没有（10:30 又自动补了 31 首站内栏目分类，是从"提案路径漏歌"的 bug 修出来的） | `_analysis/tag_todo.tsv`（`--emit-template` 重新导出的新清单） | 填 `human_tag` 列 → `python3 jianpu2/tools/propose_tags.py --from-tsv _analysis/tag_todo.tsv` | 先批处理最省事的：`_analysis/tag_todo_借标签.tsv` 里 **3 首**已按"同一首的另一版本有标签"预填好，扫一眼即可落地 |
| 2 | **曲名改名** | **138 条**高置信度 | `_analysis/title_proposal.tsv`（275 条提案，`accepted` 列） | 审完把同意的留 `1` → `python3 jianpu2/tools/refine_titles_from_pages.py --apply` | 工具**已隔离实测**（标题+文件名一起改、`todo` 清掉、真语料 0 变化），可以放心跑 |
| 3 | **「同名不同曲」消歧** | **183 组**（248 个"多版本"标题里 169 组版本间毫不相似） | `_analysis/同名不同曲_2026-09-24.md` + `同名不同曲提案.tsv` | 给通用曲名（爱/家/谁/海阔天空…）补 `alias=歌手`，或让 `group_of` 对 1–2 字短名用 `usertag` 区分 | 先只修**榜单命中**的那批（cma 4 / cn_pop 13 / kugou 19），影响面最大 |
| 4 | **改编/器乐谱收不收** | **1,835 首**被纯度门挡下 | `_analysis/arrangement_candidates.tsv` | 你的口径决定（收 → 需要重转写；不收 → 保持现状） | 我倾向**先不收**：它们大多是吉他/钢琴谱，转不出"那首歌的旋律" |
| 5 | **补谱转写** | **67 首**已备好同名简谱（合计 87 首有同名谱） | `_analysis/金曲缺口与转写队列_2026-09-24.md`；自查 `python3 jianpu2/tools/check_transcribe_ready.py` | 有 GPU 的机器上：下基座 `Qwen2.5-VL-3B-Instruct`(7GB) + 装依赖（清单里给了命令），再 `python3 tools/transcribe.py <图片>` | 本机**不建议**跑（无 CUDA、6.9GB 内存）；图片与队列都就位，随时可搬 |

## 3. 这一夜我修掉/建起来的东西（按性质）

**会静默吃掉数据或改坏仓库的（都修了 + 都补了自检）**
* 投稿漏斗 5 个坑：手打空格的数字串被丢、`git commit -- <新文件>` 不含未跟踪文件、list 标签 500、留档 id 撞车、丢字符不告警；另加并发 `GIT_LOCK`、纠错目标、`jptok` 变硬依赖（不再静默换兜底口径）
* `parse()` 的副作用：解析一份 /tmp 副本会污染已提交的 `by_*` 链接树（现在加护栏）；两处"跟着 cwd 走"的依赖；标签/标题两个**注入**口（换行字符）
* `songs.jsonl.gz` 每次 refresh 都变（gzip 时间戳）→ 固定 mtime，现在跑一整轮 refresh 两个仓库都是 **0 处改动**
* **人工补标签这条路曾经是断的**：`--emit-template` / `--from-tsv` 因我上次重构误删函数而 NameError（`--help` 冒烟走不到那行）→ 修回 + 加了静态/功能两道防线

**建起来的验证**
* `check_tools.sh` 五层（冒烟／静态未定义名／**四条写盘功能自测**（标签·收录页·标题·**隔离不许污染工作台**）／解析副作用）+ 新工具 `check_undefined.py`
* 测"动文件"的两个工具（`quarantine_short_scores` / `audit_corpus_quality`）：隔离副本里跑通（只移不删、真语料 0 变化）；修了 quarantine 重复跑误报 + **我自己误把工作台提案覆盖**的问题（新增 `safeout.py` 护栏 + 自测）
* `check_jptok_parity.py`：两份 token 口径用 **723 万 token** 证明逐项一致
* `check_parity.sh`：Python 榜单查询在**浏览器 JS** 侧重放，四榜单 Top-3/5 全 100%
* `corpus_fingerprint.py`：批量改语料的安全网（存/比指纹，键集合变了就报错）
* 部署原件入库 `misc/records/deploy/`（单元 + drop-in + 安装步骤）

## 4. 我已经验证过的（你不用再验）

refresh 幂等；4 榜单无回归；JS 与 Python 等价；**可转写队列的图 100% 干净**（317 张逐张验过；历史图里 1.3% 是不可用的细条/坏图，但都不在可转写子集里）；**`待整理已跳过 499` 查清了**（273 个 `midi` 多轨转储，中位数 11,242 音、合计 474 万音 = 语料的 3.6 倍，必须排除；225 个无 status 的东方元数据空壳，0 音符）；自检门加了第 7 条把这个不变量钉住；**拍号/小节从语料内部判不了**（不写小节线 + 弱起让总拍数不可辨识，只有 3 首含 `|` 的那几个文件有异常小节显示，旋律不受影响）；45,075 个 `by_*` 链接全部指向 `scores/`；`data.jsonl` 重跑逐字节一致；
抢救包按 `_manifest.txt` 核对完整（images 92M / models 1.1G / images-prep 8.9G 都已解出来）。

## 5. 我说错过两件事（已更正，别被我误导）

1. **"`models/` 是空的"** —— 只看 `jianpu2/models/` 就下了结论。实际工作区根 `models/` 里有 **1.1GB 的 LoRA 适配器 + 网格检测器**；转写缺的只有基座模型(7GB)与 Python 依赖，**真正卡的是硬件**。
2. **"旧清单的图随旧机器丢了"** —— 旧爬的 82 个批次在 `../images-prep/`（8.9GB）里，我那次重爬是**冗余**的。

## 6. 命令速查

```bash
bash _analysis/推送.sh --check && bash _analysis/推送.sh     # 推三个库（要凭据）
HF_TOKEN=hf_xxx bash _analysis/上传HF.sh                     # 传 HuggingFace
bash jianpu-web/tools/check_all.sh                           # 前端+接口 6 组自检
JIANPU_QUICK=1 bash jianpu2/tools/check_tools.sh             # 工具链（跳过 2-3 分钟的口径那步）
python3 jianpu2/skills/jianpu-melody-lookup/selfcheck/selfcheck.py   # 语料门 6 条
python3 jianpu2/tools/check_transcribe_ready.py              # 转写还缺什么
python3 jianpu2/tools/corpus_fingerprint.py --check jianpu-db/misc/records/fingerprint_20260924-0411.json  # 语料有没有被动(最新基线)
```

*（本页由 DSH 于 2026-09-24 09:20 汇总七轮夜间工作生成；细节在各专题记录里。）*
