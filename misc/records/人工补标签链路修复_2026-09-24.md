# 人工补标签这条路**曾经是断的** —— 修复记录（2026-09-24 05:55–06:20）

> 这条链路是"给 941 首无标签的歌补标签"的唯一人工入口：
> `--emit-template` 导出待补清单 → 人填 `human_tag` 列 → `--from-tsv` 写回曲谱。
> 它**两个命令都崩**，而且崩了很久没人发现。

## 1. 症状与根因

```
$ python3 tools/propose_tags.py --from-tsv _analysis/tag_todo.tsv
NameError: name 'apply_tsv' is not defined
$ python3 tools/propose_tags.py --emit-template
NameError: name 'emit_template' is not defined
```

根因：上一次重构 `336a2a9「propose_tags.add_tag 改为委托 jianpu-db/linkurl.add_usertag(口径只有一份)」
把 `add_tag` 换成了委托调用，但**顺手把 `apply_tsv` 与 `emit_template` 一起删了，而 `main()` 里的调用还在**。
（这是我的锅：那次重构是我做的，而当时的"自检"只跑了 `--help`。）

**为什么没人发现**：`check_tools.sh` 只跑每个工具的 `--help`，而 `--help` 在 argparse 就退出了，
**永远走不到 `main()` 的调用行**。这类 bug 只有真跑一遍才会暴露。

## 2. 修法

| 文件 | 修了什么 |
|---|---|
| `tools/propose_tags.py` | 补回 `apply_tsv`（逐行读 `file` + **最后一列** `human_tag`，写盘走 `add_tag` → `linkurl.add_usertag` 唯一实现；空 `human_tag` 跳过；非法标签报错但继续；带新增计数）与 `emit_template`（导出行与 `--from-tsv` 口径一致）；`JIANPU_DB` 指错时报"找不到 xxx/linkurl.py —— JIANPU_DB 指对了吗?"而不是 `No module named 'linkurl'` |
| `tools/eval_beam_hg.py` | 缺 `import re`（顺手，同一个静态检查发现的） |

**实测**（隔离副本，不碰真语料）：写入 2 条 → 再跑一次全部 `已存在`（幂等）；`todo=add tags` 被清掉；
拿真的 `_analysis/tag_todo.tsv`（全空 `human_tag`）跑 → **0 写入、语料 0 变化** ✓。

## 3. 新增的两道防线（这类 bug 以后会被逮住）

1. **静态**：`tools/check_undefined.py` —— 用 `ast` 找"读取了但模块里没定义"的名字
   （def/class/赋值/import/参数/except/global/with 都算定义）。实测扫 **574 个 .py：0 问题**
   （`parse_scores.py` 因为 `from score import *` 跳过）。
   它**本来就能逮住这次的 bug**（改之前跑会报 `propose_tags.py: 未定义 apply_tsv / emit_template`）。
2. **功能**：`check_tools.sh` 里加了一段"在隔离副本里真的写一次标签"的自测：
   写成功 + 幂等 + 不碰真语料，三条都验。现在 `bash tools/check_tools.sh` = 冒烟 + 静态 + 功能 + 副作用。

## 4. 顺带：`_analysis/tag_todo.tsv` 已过期

* 旧文件：**1,205 行**（上一夜导出的）；
* 现在重新导出：**941 首**（表头一致，口径是"没有 `usertag` 且挂 `todo=add tags`"）。
* **要用就用新导出的**：`python3 jianpu2/tools/propose_tags.py --emit-template`（默认就写 `_analysis/tag_todo.tsv`）。
* **已经重新导出了**（2026-09-24 07:05）：**941 首**；旧文件备份为 `_analysis/tag_todo_旧1205行_20260924.tsv.bak`；
  空跑 `--from-tsv` 验证：0 写入、语料 0 变化。

## 5. 附：3 首"借标签"提案（旋律克隆审计的严格档）

旋律克隆审计（`audit_melody_clones.py`）里，**只有"短的那首被另一首 >=90% 包含"**才建议借标签
—— 那才是"同一首的两个转写"。严格档下只有 3 首，且证据很硬：

| 待补文件（无标签） | 借的标签 | 证据 |
|---|---|---|
| `小燕子_2.txt` | `儿歌` | 与 `未命名-jianpujia-15773.txt`(儿歌) 旋律 268 音 / 99% 覆盖 |
| `对__徽______100__谱-_谱.txt` | `分类/民歌` | 与《对花安徽民歌经典民歌100首》173 音 / 100% |
| `对_河.txt` | `分类/民歌` | 与《对花河北民歌经典民歌100首》88 音 / 100% |

文件：`_analysis/tag_todo_借标签.tsv`（与 `tag_todo.tsv` **同一格式**，`human_tag` 已预填）→
你扫一眼，删掉不同意的行，然后：

```bash
python3 jianpu2/tools/propose_tags.py --from-tsv _analysis/tag_todo_借标签.tsv
python3 jianpu-db/parse_scores.py          # 重建索引(或等 refresh)
```

> 注：**第一版提案我写错了**：把"只有 30% 覆盖率的重合"也算成可借，结果出现
> 《泼水歌》→《大白鹅》、《红树林之歌》→《红树林》这种**拿不同歌的标签乱借**。
> 已把规则收紧到">=90% 包含"，并在工具里留了注释说明为什么（30% 重合只代表共享乐句）。

*（本记录由 DSH 于 2026-09-24 06:20 生成；配套：`旋律克隆与重复_2026-09-24.md`。）*

## 6. 顺带加固：标题改名这条写盘路径（同一个坑的另一半）

`refine_titles_from_pages.py --apply` 是"138 条曲名改名"的落地命令，也要做同样的检查：

* **读了一遍代码**：它是拿 `re.sub(r"(?m)^title=.*$", "title="+new, text)` 直接替换的，
  而 `new` 来自**网页标题**（不可信输入）—— 如果标题里带换行，就会往曲谱里**注入额外元数据行**
  （与标签那个坑同一类）。现有 275 条提案实测干净，但**来源是网页，必须在写入前兜住**。
* **修法**：新增 `clean_title()`（去 `\r\n\t`/控制字符、合并空白），在**生成提案时**和**落盘前**
  各清一遍；替换改用 `lambda m: "title=" + new` —— 这样 `new` 里的 `\1`/`\g<…>` 也不会被
  当成正则替换引用（原来的写法有这个隐患）。
* **隔离实测**（用真缓存、只动 /tmp 副本）：`title=牧羊姑娘1=F4_4_深情悠扬地` → `title=牧羊姑娘`，
  文件名同时改为 `牧羊姑娘.txt`，`todo=refine the filename` 被清掉；**真语料 0 变化** ✓。
* `check_tools.sh` 再补一条功能自测：隔离副本里跑 `--offline`，必须正常出提案（275 条）。

现在 `bash jianpu2/tools/check_tools.sh` 覆盖四层：**冒烟（--help）+ 静态（未定义名）+ 功能（标签写回、
标题提案）+ 解析副作用**。这一夜三次踩到"自检没覆盖真路径"的坑，这是补上的部分。
