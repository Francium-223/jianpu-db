feat: data.jsonl 每首谱都有**显式小节线**(按拍号+时值恢复)

用户口径: 「进 data.jsonl 的所有小节线必须是显式的。」

现实: 全库只有 2.5%(197/7884)的谱真写了 `|`, 且 expand 步会吃掉 R{..} 里的 —— 所以
不能靠"保留", 改为**确定性恢复**:
  ① 拍号 n/d -> 每小节拍数 = n*4/d
  ② 音符拍值: h=2, 无前缀=1(四分), q=0.5, s=0.25, d=0.125; 附点 x1.5; `-` 记 1 拍
  ③ 累加到每小节拍数就落一条线并归零
  ④ 源文件里已有的 `|` 当强小节线, 累加器强制归零(处理弱起/不规则小节)
新增字段 `bars`(0-based 音符下标, 表示该音符之前有小节线) 与 `beats_per_bar`。
实测: 7384/7385 首有 bars, 共 339,475 条; 义勇军进行曲(2/4) 每 2 拍一条, 与谱面一致。

Co-authored-by: deepseek-ai <service@deepseek.com>
